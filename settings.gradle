pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "Coding Challenge"
include(":app")
include(":core")
include(":podcast")
include(":podcast:podcast_data")
include(":podcast:podcast_domain")
include(":podcast:podcast_presentation")
